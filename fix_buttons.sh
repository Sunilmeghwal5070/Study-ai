sed -i 's/PrimaryButton(/IconButton(onClick = { tts?.speak(answer!!, TextToSpeech.QUEUE_FLUSH, null, null) }) { Icon(Icons.Default.VolumeUp, contentDescription = null, tint = MaterialTheme.colorScheme.primary) }\n                            IconButton(onClick = {\n                                val clipboard = context.getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager\n                                clipboard.setPrimaryClip(ClipData.newPlainText("Answer", answer!!))\n                            }) { Icon(Icons.Default.ContentCopy, contentDescription = null, tint = MaterialTheme.colorScheme.primary) }\n                            IconButton(onClick = {\n                                val sendIntent = Intent().apply { action = Intent.ACTION_SEND; putExtra(Intent.EXTRA_TEXT, answer!!); type = "text\/plain" }\n                                context.startActivity(Intent.createChooser(sendIntent, null))\n                            }) { Icon(Icons.Default.Share, contentDescription = null, tint = MaterialTheme.colorScheme.primary) }\n                            PrimaryButton(/g' app/src/main/java/com/example/ui/screens/SolverScreen.kt
